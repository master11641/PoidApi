@model Food

@{
    ViewData["Title"] = "Delete";
     Layout=null;
}

<div class="alert alert-danger"  style="padding: 25px;">
    <span>آیا از حذف غذا اطمینان دارید ؟</span>
    <form asp-action="Delete" id="foodFrm">
        <input type="hidden" asp-for="Id" />
        @* <input type="submit" value="Delete" class="btn btn-danger" /> | *@
        <a class="btn btn-primary btn-lg  tjax" href='@Url.Action("Delete","Food")' forsendform="#foodFrm"
                    method="POST" record="false">حذف غذا</a>
    </form>
</div>
